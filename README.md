Если не установлен yarn:
    # ~ npm i -g yarn

Для старта :

- дериктория client :
    # ~ yarn install
    # ~ yarn start

- дериктория server :
    # ~ yarn install
    # ~ yarn dbr
    # ~ yarn dev
